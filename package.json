{
  "name": "fairplay",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ fairplay",
    "create-local": "graph create --node http://localhost:8020/ fairplay",
    "remove-local": "graph remove --node http://localhost:8020/ fairplay",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 fairplay",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.80.0"
  },
  "devDependencies": {
    "@graphprotocol/graph-ts": "^0.31.0",
    "matchstick-as": "0.5.0"
  }
}
